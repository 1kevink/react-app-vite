export enum Variants {
  PRIMARY = 'primary',
  QUATERNARY = 'quaternary',
  SECONDARY = 'secondary',
  TETRIARY = 'tetriary',
}

export enum ButtonTypes {
  BUTTON = 'button',
  RESET = 'reset',
  SUBMIT = 'submit',
}

export enum ButtonSizes {
  LARGE,
  MEDIUM,
  SMALL,
}
